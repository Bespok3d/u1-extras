# Changelog

## 0.1.0

- First release. Discover-and-run hooks for PRINT_START/PRINT_END/CANCEL_PRINT via prefix-named
  macros, run in sorted order (number them to sequence). Reimplemented as a config override rather
  than a fluidd.cfg patch. Experimental; ordering is name-based by design (see the danger note).
